import { sendMessageAction } from "./sendMessage";

// Exportamos las acciones individuales para uso directo
export { sendMessageAction };

// Exportamos todas las acciones como un namespace
export * as actions from "./sendMessage";
